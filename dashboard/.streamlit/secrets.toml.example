# Copy to .streamlit/secrets.toml and fill in real values. Never commit the
# populated file -- it holds the Supabase service-role key, which bypasses
# row-level security.
[supabase]
url = "https://YOUR_PROJECT.supabase.co"
secret_key = "YOUR_SERVICE_ROLE_KEY"
